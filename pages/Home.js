import { ScrollView, Text } from 'react-native';

// successful authentication -> route here
export function Home({ navigation }) {
  return (

    <ScrollView>
        <Text>

            LOGGED IN USERS COME HERE

        </Text>

    </ScrollView>

  );
}